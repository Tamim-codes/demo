1.configuring git :
git config --global user.name"My Name"
git config --global user.email"someone@email.com"
git config --list
           
            GITHUB TO LOCAL MACHINE
2.Clone & Status: 
clone - cloning a repository on our local machine
         git clone<-some link->

3.status -displays the state of the code
         git status

(a) untracked :new files that git doesn't yet track
(b) modified : changed
(c) staged :file is ready to be committed
(d) unmodified :unchanged


4.cd foldername=change directry
  cd .. (exist the folder)
  mkdir filename (new file or directory)

5.ls = folder list

6.ls -a = all folder list


7.Add & committed
# add -adds new or changed files in your working directory to the fit staging area.
    git add <-file name->
    git add . (add all files)

# commit -it is the record of change
   git commit -m"some message"

8.Push command
             (laptop)              (github)
push -upload local repo content to remote repo
              (JAITA CLONE KORHA HOISA)     (branch)
     git push           origin                main


            Local machine to github

1.Init command
init -used to create a new git repository
git init

       and 
check : dir -Force
git rm --cached filename

